box: wercker/python
build:
  steps:
    - pip install --upgrade pip
    - pip install --upgrade setuptools
    - pip-install
    - script:
        name: s3sitedeploy Tests
        code: make test
    - script:
        name: Cleanup
        code: make clean
